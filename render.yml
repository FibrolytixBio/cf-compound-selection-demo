services:
  - type: web
    name: fastapi-backend
    env: python
    plan: free
    buildCommand: |
      cd backend
      pip install uv
      uv venv
      uv sync
      uv pip install -e .
    startCommand: |
      cd backend
      uv run uvicorn api:app --host 0.0.0.0 --port $PORT
